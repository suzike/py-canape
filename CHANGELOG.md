# 变更记录

## 3.4.2 - 2026-07-30

- 新增 `agent2canape mcp-doctor`，分层检查 MCP SDK、pywin32、默认工程、Windows COM
  环境、Codex 注册、Claude Code 连接和模型端点类型；
- 支持 `--live-canape` 真实打开工程并执行只读项目信息与设备列表调用，不触发 ECU
  标定写入、诊断或刷写；
- 针对 CANape 17 偶发的 Asap3 句柄和共享内存启动竞态执行一次受控重试，并在报告中
  明确记录尝试次数与是否由瞬态故障恢复；
- 诊断报告仅输出模型端点主机和认证存在状态，不输出令牌值或客户端原始错误流；
- Codex 与 Claude Code 示例补充 `WINDIR`/`SYSTEMROOT`，Codex 冷启动与工具超时统一
  提升到 120 秒；
- 新增 MCP 诊断、客户端配置缺口、敏感信息保护与 CLI 测试，累计 117 项自动化测试。

## 3.4.1 - 2026-07-30

- MCP 新增 `AGENT2CANAPE_DEFAULT_PROJECT`，在第一个 CANape 工具调用时懒加载受信任工程，
  避免 CANape 17 在尚未执行 `Open2` 时读取项目属性导致 COM 进程异常退出；
- MCP 新增 `AGENT2CANAPE_MCP_TOOL_ALLOWLIST`，按注册表名称限制工具发现、清单和自然语言
  规划结果，兼容第三方模型代理并支持项目级最小权限暴露；
- Codex 与 Claude Code 配置示例补充默认工程参数和 Windows 实际接入说明；
- 新增默认工程懒加载与 MCP 工具允许列表测试，累计 111 项自动化测试。

## 3.4.0 - 2026-07-30

- 新增 MAP X/Y 轴物理梯度和网格对角邻域约束，并接入标定计划候选校验；
- 新增可插拔物理模型约束，支持项目派生指标、必需输入和安全上下界；
- 新增 DOE 环境稳态门禁，检查有效样本、波动范围、线性斜率、工况范围和时间轴；
- 新增单 Case 指标验收及 MAD、Z-score、IQR 队列级异常剔除；
- 实验存储升级为 schema v2，持久化质量详情、拒绝原因和异常规则结果；
- 新增 JSON/Markdown 实验报告，提供状态、统计摘要、拒绝/失败原因和证据索引；
- 新增纯 Python 高斯过程代理模型，使用 RBF 核、Cholesky 分解和目标置信界；
- 新增安全候选筛选，强制安全指标置信边界、最大外推距离和历史候选去重；
- CLI 增加 `calibration-experiment-report` 和 `calibration-safe-suggest`；
- AI/MCP 增加实验报告、安全候选推荐工具及自然语言路由；
- 新增无 Mermaid 图文指南、可运行示例及 11 项决策层与 AI 自动化测试。

## 3.3.0 - 2026-07-30

- 新增 `CalibrationTargetAdapter`，统一 RAM/ROM 捕获、应用、持久化、恢复和重连协议；
- 新增 `CANapeCalibrationTarget`，直接接入 CANape RAM 标定 API，ROM 动作通过车型项目
  回调显式适配，未配置时不会虚报持久化成功；
- 新增 Working→RAM→ROM 持久化协调器，包含职责分离、内容摘要、原子作业日志、
  幂等重入和跨进程设备占用锁；
- 新增写后掉线状态协调：重连后先回读判断动作是否已生效，避免盲目重复写入；
- 新增 RAM/ROM 逆序补偿、补偿回读和 `recovery_required` 人工恢复升级；
- 新增多 ECU RAM 屏障和 ROM 分阶段提交，任一失败时跨设备逆序恢复；
- 新增确定性故障注入目标，覆盖写前失败、写后掉线、重连和补偿失败；
- CLI、AI/MCP 增加持久化作业状态查询与自然语言路由；
- 新增无 Mermaid 图文指南、可运行示例及 14 项目标执行层自动化测试。

## 3.2.0 - 2026-07-30

- 新增按功能组、页面、责任人和风险组织的标定变更集，支持职责分离、多人审批和评审摘要；
- 新增 Working、Reference、RAM、ROM 快照台账、层间差异和持久化一致性门禁；
- 新增 DOE 原子检查点、中断恢复、失败重试、每轮基线恢复及证据文件 SHA-256 校验；
- 新增多 ECU 标定协调器，所有设备写前预读，任一失败时执行跨控制器全局逆序回滚；
- 新增多目标 Pareto 分析，先应用安全边界，再计算非支配候选和平衡候选；
- CLI 增加 `calibration-review`、`calibration-memory-status` 和
  `calibration-experiment-status`；
- AI/MCP 增加变更集、存储层、DOE 状态和 Pareto 分析只读工具及自然语言路由；
- 新增生产标定作业与多 ECU 协同图文指南及 11 项操作层与 AI 自动化测试。

## 3.1.0 - 2026-07-30

- 新增 A2L 语义目录，解析项目/模块、字节序、转换方法、记录布局、测量量、标定量和轴描述；
- 新增 CDFX、DCM 和确定性文本 PAR 的标量、曲线、MAP、ASCII、轴与单位导入导出；
- 新增车辆、ECU、软件、标定、A2L SHA-256 和 HEX SHA-256 身份绑定与一致性校验；
- 新增范围、梯度、单调性和标量联动约束，并接入标定变更计划的候选结果校验；
- 标定版本仓库增加跨进程写锁、并发原子提交、基线冻结状态和全仓完整性巡检；
- CLI 增加 `a2l-summary`、`calibration-convert` 和 `calibration-verify`；
- CANape COM 创建改为强制动态晚绑定，避免损坏的 pywin32 `gen_py` 缓存导致
  `Invalid Asap3 Handle`；
- 新增生产级标定数据图文指南及 CDFX/DCM/PAR、身份、约束和并发测试。

## 3.0.0 - 2026-07-30

- 产品、Python 发行包、命名空间、CLI、MCP、环境变量、插件入口和能力契约统一更名为
  `Agent2Canape` / `agent2canape` / `A2C-NNN`；
- 移除 README 中指向旧代码仓库的克隆链接，保留正常产品与官方文档链接；
- 工作流写动作改为默认拒绝，必须显式使用 `allow_writes=True` 或 `--allow-writes`；
- 修复整型 CANape 测量状态无法通过 AI 工具序列化的问题；
- 修复 JSON Schema 将布尔值误当作整数/数值参数的问题；
- 修复线程安全工作流超时后仍等待后台动作结束的问题；
- 增加快照/恢复目录边界保护、标定版本路径保护和 SHA-256 加载校验；
- 修复状态振荡识别、陈旧响应误配及零时刻异常聚类问题；
- 证据包遇到同名文件时显式失败，避免 ZIP 内静默覆盖。

## 2.0.0 - 2026-07-30

- 将产品主线升级为完整 ECU 标定工具：支持标量、枚举、ASCII、曲线、轴和二维 MAP；
- 新增标定数据集 JSON/CSV、哈希、差异、补丁、三方合并和版本仓库；
- 新增标定变更计划、审批、基线锁定、事务写入、回读、失败回滚和会话恢复；
- 新增全因子、OFAT、Latin Hypercube 标定实验和带边界的坐标搜索优化；
- 新增面向 Codex、Claude Code 等 AI Agent 的结构化工具、自然语言规划和 stdio MCP；
- 新增跨进程 Action Plan 存储、工具与参数摘要绑定、外部审批、原子认领和单次执行；
- 扩充 AI 工具至项目、设备、测量、标定、内存、诊断、Tester Present、网络和刷写；
- 新增 ODX/PDX 目录解析、MDF 提取导出、跨源信号字典和时间源同步报告；
- 新增策略量化、可解释异常聚类、工作流失败分类、补偿、多 ECU 编排和 CI 摘要；
- 将七个内置领域包从空占位升级为信号别名、质量规则、指标和能力依赖齐全的适配器；
- 将 140 项注册从“函数存在”升级为唯一行为契约、明确验收文本和可解析实现验证；
- 新增图文 AI 联动文档、Codex/Claude Code 配置示例和非 Mermaid 架构图。
- 修复 CANape `Open2` 第五参数语义反转：非模态调用现在传 COM `modalmode=0`，
  避免自动化脚本一直阻塞到 CANape 关闭。

## 1.0.0 - 2026-07-30

- 为 1—140 项能力建立机器可读注册表和可调用实现绑定；
- 新增工程资产、环境预检、SHA-256 清单、项目快照和恢复；
- 新增 CSV/JSON/Parquet/Excel、可选 MDF/BLF/A2L/DBC 数据层；
- 新增统一信号字典、重采样、时间对齐和通道覆盖率检查；
- 新增范围/变化率/冻结/断点、状态机、因果链、滞环、控制性能、能量和 A/B 分析；
- 新增 YAML/JSON 工作流、变量覆盖、重试、超时、检查点、Dry-run 和批量回归；
- 新增权限分级、对象/地址白名单、车辆前置条件和秘密提供协议；
- 新增哈希链审计、证据包、Word/Excel/PDF/HTML 报告和问题单适配协议；
- 新增领域插件协议与动力、底盘、车身、三电、热管理、ADAS、网络诊断内置领域包；
- 新增架构文档和三项 ADR，明确模块边界、可选依赖和写操作安全策略。
- 通过 A/B 回归将 CANape 17 的 Windows COM 依赖约束为 `pywin32<312`，并记录
  vMDM 驻留进程导致共享内存未及时释放的生命周期限制。

## 0.2.0 - 2026-07-29

- 将开发工作空间迁移到 `E:\Agentic_Engineering\Codex\Vectoer工具AI化\Agent2Canape`；
- 增加测量缓冲、任务整帧/FIFO 采样和 ECU 内存接口；
- 增加批量标定快照、差异、写后回读和失败回滚；
- 增加记录器、网络、API 日志、脚本、诊断、Tester Present、安全配置和刷写状态接口；
- 对测量启停超时改为显式异常，避免自动化流程静默继续；
- 兼容 pywin32 对 COM 返回值/out 参数的不同排列；
- 区分 CANape 产品版本 `17.0.31` 与 COM API 版本 `2.3`；
- 修复 CLI 自检、项目打开失败和退出时的 CANape 实例生命周期；
- 扩充模拟 COM 单元测试，并建立 140 项通用整车工程能力路线图。

## 0.1.0 - 2026-07-29

- 建立 `CANape.Application` COM 控制包；
- 支持项目、测量、设备、任务/通道、标定、记录器和脚本接口；
- 增加 CLI、基础示例、单元测试和 CANape 17 只读冒烟脚本；
- 适配并记录 CANape 17.0.31 COM 接口行为。
