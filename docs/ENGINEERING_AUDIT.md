# Agent2Canape 最终工程审查

审查日期：2026-07-30

审查对象：`src/agent2canape`、测试、示例、脚本、构建配置和全部交付文档

审查视角：真实 ECU 标定、在线测量、整车问题排查、AI/MCP 编排、数据闭环和发布维护

## 结论

Agent2Canape 已形成可安装、可测试、可扩展的 CANape 自动化与 ECU 标定软件基线，
但还不能把“140 个注册契约可解析”解释为“140 项均已在真实 ECU 上完成行为验收”。
当前适合作为工程开发基线和企业适配起点；涉及在线写入、诊断、刷写、显式 CNA 加载、
大规模数据性能和企业审批的能力，仍需真实项目与硬件验证。

## 本轮已修复

| 问题 | 风险 | 处理 |
|---|---|---|
| 产品名只改文档会形成多套接口 | 高 | 发行包、命名空间、CLI、MCP、环境变量、插件组和契约编号统一更名 |
| 工作流 `write=True` 只有标记、没有执行门禁 | 严重 | 默认拒绝写步骤，必须显式 `allow_writes=True` 或 `--allow-writes` |
| 整型测量状态无法被 AI 工具序列化 | 高 | 对 COM 整型状态返回稳定结构 |
| JSON Schema 把 `bool` 当成地址或数值 | 高 | 整数和数值参数显式拒绝布尔值 |
| 工作流超时后上下文管理器仍等待线程 | 高 | 超时立即返回并取消尚未开始的任务 |
| 快照恢复可覆盖危险目录 | 严重 | 拒绝根目录、用户目录、同目录和嵌套目录 |
| 标定版本名可用于路径穿越，加载不验哈希 | 严重 | 限制版本名，原子写文件，加载时校验清单 SHA-256 |
| 标定仓库并发提交可能丢失清单记录 | 严重 | 增加跨进程写锁、唯一临时文件、原子提交和全仓巡检 |
| A2L/HEX/软件/标定身份可能错配 | 严重 | 增加文件 SHA-256 与车辆、ECU、软件、标定身份绑定和校验 |
| 标定数据交换仅支持 JSON/CSV | 高 | 增加 CDFX、DCM 和确定性文本 PAR 适配器 |
| 标定曲线和联动关系缺少工程约束 | 高 | 增加范围、梯度、单调性和标量联动约束并接入计划预览 |
| 标定变更缺少功能组、页面和责任人评审视图 | 高 | 增加结构化变更集、职责分离和可配置多人审批 |
| DOE 中断会丢失进度与证据关系 | 高 | 增加原子检查点、恢复、重试、基线复原和证据哈希 |
| 多 ECU 逐个写入失败后无法整体恢复 | 严重 | 增加全量预读、顺序提交和跨控制器全局逆序回滚 |
| RAM/ROM 台账没有真实目标动作协议 | 严重 | 增加可插拔捕获、写入、持久化、恢复和重连协议 |
| 写操作已生效但响应前掉线会导致盲目重试 | 严重 | 重连后先回读协调不确定动作，目标已生效则不重复写入 |
| 多 ECU 未全部验证 RAM 就开始 ROM 持久化 | 严重 | 增加全设备 RAM 屏障、ROM 分阶段提交和逆序补偿 |
| 两个 AI/终端可同时控制同一持久化作业 | 严重 | 增加跨进程占用锁、进程存活检查和显式陈旧锁恢复 |
| MAP 扁平梯度无法识别二维局部尖峰 | 高 | 增加 X/Y 物理梯度和网格对角邻域约束 |
| 候选只满足数值边界但违反系统物理关系 | 严重 | 增加可插拔项目物理模型和派生指标安全边界 |
| DOE 在环境漂移或异常样本下仍判通过 | 高 | 增加写前稳态、指标验收和 MAD/IQR/Z-score 队列判定 |
| 代理优化可能向未知危险区域外推 | 严重 | 增加安全置信界、安全锚点距离和历史候选去重 |
| 已经为高电平的响应被误判为新响应 | 高 | 在完整时间轴上识别边沿后再配对触发 |
| 两状态往返振荡漏报 | 中 | 修正振荡窗口边界 |
| 零时刻异常窗口计算错误 | 中 | 不再把 `0.0` 当成缺失值 |
| ZIP 证据包同名文件静默覆盖 | 高 | 检测同名并显式失败 |

工作流超时只能停止等待，Python 线程不能强制终止已经运行的硬件调用。因此 CANape COM
动作仍被标记为 `thread_safe=False`，超时后需要由动作自身实现取消、停止或恢复策略。

## 模块级审查

| 模块 | 当前可用性 | 主要剩余问题 |
|---|---|---|
| `canape.py` | 会话、设备、测量、标定、诊断、网络和刷写 API 已覆盖 | 指定 CNA、在线写入、诊断和刷写缺少多版本实机矩阵 |
| `calibration.py` | 数据集、身份、通用约束、并发版本、计划、回滚、DOE 和优化可用 | 缺少签名标签 |
| `calibration_design.py` | 二维/物理模型约束、DOE 质量、实验报告和安全高斯过程可用 | 项目公式、阈值和真实台架样本仍需车型验证 |
| `calibration_formats.py` | A2L 语义目录及 CDFX/DCM/PAR 常用对象交换可用 | ASAP2 全关键字和供应商私有格式仍需项目样本适配 |
| `calibration_operations.py` | 变更评审、存储台账、DOE 恢复、多 ECU 回滚和 Pareto 可用 | 签名审批仍需企业适配 |
| `calibration_targets.py` | 目标协议、CANape RAM 接入、ROM 回调、掉线协调、分阶段提交和故障注入可用 | 项目 ROM 回调与真实掉线/断电仍需 ECU 台架验收 |
| `ai_tools.py` | 结构化工具、对象/单位上下文、实时写入预览、前置状态绑定和单次审批可用 | 复杂复合意图仍需上层模型拆分；审批身份不是企业级可信身份 |
| `mcp_server.py` | FastMCP、会话标识、限流、跨进程 CANape 租约和摘要审计可用 | 长任务进度/取消和企业审计汇聚仍待实现 |
| `workflow.py` | 重试、检查点、补偿、Dry-run 和写门禁可用 | 已启动线程不可强制取消；缺少步骤级幂等键和分布式执行 |
| `assets.py` | 预检、清单、快照和恢复可用 | 缺少 CANape 许可证、Vector 驱动、硬件通道和 A2L/HEX 一致性深检 |
| `offline.py` | 表格、MDF、BLF、DBC、语义 A2L 和基础 ODX 目录可用 | ODX 仍为轻量目录；A2L 企业扩展关键字需样本验收 |
| `analysis.py` | 通用质量、时序、状态、控制和对比分析可用 | 缺少不规则采样置信度、事件配对策略、统计显著性和因果证据等级 |
| `safety.py` | 权限、对象/地址白名单和车辆条件可用 | 底层专家 API 可绕过；缺少双人审批、签名策略和安全状态快照 |
| `reporting.py` | 审计链、ZIP、HTML、Word、Excel 和基础 PDF 可用 | PDF 不适合中文企业版式；审计链缺少并发锁、数字签名和可信时间戳 |
| `plugins.py` | 七领域适配器和入口发现可用 | 内置规则仅为示例基线，不能替代车型级信号与阈值包 |
| `capabilities.py` | 140 个编号、名称和实现入口可校验 | 当前验证证明“入口存在”，不等于 140 个独立行为均被测试 |
| `cli.py` | 自检、能力、资产、工作流和 AI 操作可用 | 缺少统一日志等级、JSON 错误协议、取消命令和运行历史查询 |
| `platform.py` | 提供模块组合和默认动作注册 | 需要项目模板、生命周期容器和统一运行上下文 |
| `models.py` / `errors.py` | 稳定类型和异常分层可用 | COM 错误仍需补充 HRESULT、设备、步骤和恢复建议字段 |

## 真实项目必须补齐的 P0 能力

1. 建立 CANape 17/18/19、不同补丁版本和 32/64 位组合的兼容矩阵。
2. 在真实 XCP on CAN、XCP on Ethernet、CCP 和多 ECU 项目验证连接生命周期。
3. 验证指定 CNA 加载、项目切换、异常退出和 vMDM 资源恢复。
4. 对标量、枚举、曲线、MAP、轴、ASCII 和块标定做真实 A2L 回读/写入/回滚验收。
5. 建立点火、电压、车速、档位、充电状态和台架急停等写入前置条件模板。
6. 为刷写增加文件签名/校验和、供电条件、Boot 状态、断点恢复和失败救援流程。
7. 为诊断增加 NRC 分类、会话/安全访问状态机、P2/P2* 超时和 Tester Present 管理。
8. 将审批接入可信身份，至少支持操作者与审批者分离、双人审批和不可抵赖审计。
9. 所有高风险动作补充硬件在环故障注入：掉线、超时、断电、总线拥塞和回读不一致。
10. 将 140 个能力契约升级为 140 个独立验收用例，并保留真实项目证据编号。

## 下一阶段能力增强

### ECU 标定

1. 企业 CDFX/DCM/PAR 方言、模板和私有标定格式适配。
2. A2L 枚举表、函数分组、存储段、位掩码和供应商扩展关键字解析。
3. 将通用二维邻域和物理模型接口接入车型公式库、单位系统和模型版本清单。
4. 在现有功能组/页面/责任人变更集上增加企业签名审批和网页/桌面评审界面。
5. 将通用目标协议接入车型实际页面切换、下载和持久化回调，并形成 ECU 适配包。
6. 将已自动验证的掉线协调、分阶段提交和故障注入扩展到真实硬件断电与总线故障验收。
7. 将已实现的稳态、异常剔除和报告接口接入真实环境舱、台架和企业报告模板。
8. 用真实台架样本验收高斯过程超参数、安全置信倍数和专业探索边界。
9. 在现有仓库写锁、并发提交和基线冻结上增加签名标签、可信时间戳和权限适配。
10. 在现有 A2L/HEX/软件/标定/车辆身份校验上增加 ECU 在线指纹自动比对。

### 测量、诊断和刷写

11. 测量清单模板、采样事件预算、DAQ 资源估算和超限降级。
12. 触发记录、前后缓存、多记录器和录制文件闭环校验。
13. 在线信号订阅、流式窗口计算和长时间测量内存控制。
14. CAN/LIN/FlexRay/Ethernet 通道拓扑和数据库一致性检查。
15. UDS 服务模板、NRC 解释、DTC 快照/扩展数据和诊断报告。
16. Seed/Key、证书和安全访问的外部提供器协议，不在核心包保存秘密。
17. 刷写前后软件指纹、校验和、DTC、标定恢复和验收脚本。
18. 设备掉线自动重连后的测量清单、记录器和标定状态重建。

### 数据与问题排查

19. MDF 大文件分块读取、列裁剪、缓存和并行分析。
20. 实际估算多源时间偏移、漂移和时间戳不确定度。
21. 触发—响应的一对一、最近邻、超时和重复事件配对策略。
22. 缺失、重复、乱序、采样抖动、量化噪声和饱和的质量评分。
23. 回归置信区间、显著性、效应量和样本充分性。
24. 根因候选的拓扑约束、时间先后约束和证据等级。
25. 热管理、动力、底盘、车身、ADAS 等可版本化车型规则包。
26. 问题单、需求、软件版本、标定变更、测量文件和结论的追踪矩阵。

### AI 与工程平台

27. 已实现 JSON 工程上下文、常用物理单位换算、对象别名抽取和跨 ECU 消歧；后续接入
    A2L/企业知识库自动生成与检索。
28. 已实现标定写入的当前值、目标值、差异、风险、范围校验、恢复快照和前置状态绑定；
    后续扩展到内存写入、诊断和刷写的项目级预览器。
29. 已实现 MCP 会话标识、CANape COM 跨进程占用锁、单会话速率限制、摘要审计和
    运行状态查询；后续增加长任务进度、协作式取消和企业审计汇聚。
30. AI 输出的证据引用、数据时间窗和算法版本强制记录。
31. 工作流步骤幂等键、资源锁、恢复策略和可重入性声明。
32. 企业身份、审批、问题单、制品库和对象存储适配器。
33. 中文 PDF/Word 企业模板、图表、页眉页脚和自动目录。
34. 结构化日志、OpenTelemetry、运行历史和失败重放。
35. 发布物 SBOM、依赖漏洞扫描、签名 wheel 和可复现构建。
36. 公共 API 兼容性测试、弃用策略和迁移工具。

## 发布判定

发布开发基线前至少应满足：

- Ruff、完整 Pytest、`compileall`、`pip check` 和隔离 wheel 安装全部通过；
- CLI、MCP 工具发现、能力注册表、示例工作流 Dry-run 可执行；
- README 不含旧仓库链接或旧产品名；
- CANape 实机冒烟只执行只读动作，不自动终止其他工程师的全局进程；
- 发布说明明确区分软件验证、模拟 COM、实机只读、硬件写入和外部适配状态。
